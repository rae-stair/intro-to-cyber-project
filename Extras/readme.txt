Run (from this folder): pip install -r requirements.txt
Then: python app.py
Open http://127.0.0.1:5000/login

(From `Project Front End`: `python app.py` — paths may differ if you run from repo root.)
